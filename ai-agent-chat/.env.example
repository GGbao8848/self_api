# Optional. Leave empty when the frontend is served by the same self_api host.
VITE_API_BASE_URL=""

# Used only for local `npm run dev` proxying to the backend.
VITE_PROXY_TARGET="http://127.0.0.1:8666"
